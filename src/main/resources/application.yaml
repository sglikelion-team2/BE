server:
  port: 80 # 내장 톰캣 포트번호

spring:
  application:
    name: zarit

  datasource:
    url: jdbc:mysql://localhost:3306/zarit?serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: woori
    password: 1234

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# AI API Settings
ai:
  api:
    url: https://factchat-cloud.mindlogic.ai/v1/api/openai/chat/completions
    key: ${AI_API_KEY:twsFLA4VpA9cee6DCAZ81uZTYC0nQOvR}
