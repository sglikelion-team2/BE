package wooribe.zarit.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import wooribe.zarit.domain.Pin_photo;

public interface PinPhotoRepository extends JpaRepository<Pin_photo, Long> {
}
